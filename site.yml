---

-
  hosts: workstation
  gather_facts: no
  roles:
    - lookup_encrypted_vars
    - homebrew
    - mackerel-agent
    - homebrew_packages
    - zsh
    - rbenv
    - homebrew_casks
    - karabiner
    - osx-defaults
